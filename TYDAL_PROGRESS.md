# Tydal Radar — Flagship Campaign Progress

Loop memory for the autonomous improvement campaign. Newest entries on top.

## Baseline (2026-06-30)
- `tsc --noEmit`: green. `next build`: green.
- `eslint .`: **16 errors, 5 warnings** (mostly react-hooks rules: latest-ref-during-render in ClusteredMarkers/SheetShell/ProspectSheet, set-state-in-effect in useGeolocation; `require()` in the Node scraper).
- **No test framework** present (package scripts: dev/build/start/lint/gen-icons).
- Stack: Next 16 (App Router, Turbopack), MUI, Supabase (anon key), Google Maps (Places New + Advanced Markers), framer-motion, hand-written SW, Vercel.

## Deferred / open questions (NOT to be done autonomously)
- **Auth/RLS security hardening** — the Supabase anon key ships in the public bundle and RLS is `for all to anon using(true) with check(true)`, so the DB is read/write/deletable by anyone, bypassing the APP_PASSWORD UI gate. This is the biggest real gap, but touching auth is a STOP condition in the goal → **requires explicit user confirmation before implementing.**
- **Team / multi-user mode** — explicitly EXCLUDED by the user. Do not build.
- **CI via GitHub Actions** — blocked: the saved token lacks `workflow` scope, so `.github/workflows/*` can't be pushed. Logged; revisit if token is upgraded.

## Checkpoint (2026-06-30)
Safe, self-verifiable work is done for now: **tests 0→157, eslint 16→8 errors, filters/directions extracted, closed-business filtering added, CHANGELOG+progress established.** All commits verified (tsc + `npm test` + `next build`) and pushed to `main`.
Remaining items each need something I can't supply autonomously here — flagged inline below.

## Candidate queue (highest-leverage first)
1. ✅ DONE — Test infrastructure (Vitest) + pure-logic coverage (contracts/stages/icp/analytics): 136 tests.
2. 🔶 PARTIAL — Lint: safe subset done (Node-script `require` exemption, dead `eslint-disable` removed, unused `ICP` import pruned). 21→17 problems. **Remaining 15 errors are all `react-hooks/refs` + `react-hooks/set-state-in-effect`** across the interactive core (AppShell, MapView, DataProvider, ProspectSheet, ClusteredMarkers, SheetShell). Deferred deliberately: (a) latest-value-ref reads/assigns are mechanically fixable (move to `useState` lazy-init / `useEffect`), safe since refs are read only in async handlers — do per-file with care; (b) several `set-state-in-effect` cases (toasts in MapView, viewport sync in AppShell, initial load in DataProvider, layout-measure in ProspectSheet) are legitimate external-sync patterns the rule flags heuristically — fix case-by-case, do NOT mass-refactor blind (no device testing here). 2 warnings (ref-in-cleanup, missing `close` dep) also queued.
3. ✅ DONE — Data quality: `businessStatus` skip (scraper + in-app pull). (Field-refresh of mutable fields still open.)
4. Remaining 8 `set-state-in-effect` lint errors — NEEDS device testing (touches toasts/keyboard/data-load in the interactive core; won't refactor blind).
5. ✅ DONE (build-verified; device-verify pending) — Offline-first (spec: [docs/offline-first.md](docs/offline-first.md)) — **All 3 phases BUILT & pushed**: Phase 1 (reads) IndexedDB store/outbox (`lib/offline.ts`, 8 fake-indexeddb tests) + DataProvider read-through/SWR; Phase 2 (writes) optimistic outbox + flush on reconnect/foreground; Phase 3 (sync-status pill) `components/SyncStatus.tsx` (5 tests) wired into MapView top-left, fed live `{online,pending,syncing}` from DataProvider, hidden when synced. End-to-end offline UX (reads render offline, edits queue+sync, status surfaced) is built to spec + tsc/tests/build green but still NOT device-verified (needs the browser/iOS PWA — toggle offline, edit a stage across reload, confirm flush on reconnect).
6. Follow-up push notifications — NEEDS infra (VAPID keypair + a send trigger) and a product decision; can't wire fully autonomously.
7. 🔶 Accessibility — PARTIAL: dialog semantics (`role=dialog` + `aria-modal` + `aria-label`) + Escape-to-close added to the 4 sheets (via SheetShell) and the prospect card; 3 SheetShell a11y tests. Audit found icon controls / search input / action buttons already labelled. STILL OPEN (need browser/AT): focus management (move focus into dialog on open, restore on close — deferred as it risks the tuned keyboard handling), contrast measurement of muted text, and decorative map-glyph `aria-hidden` (negligible — markers aren't in the a11y tree).
8. Perf: profile ~2.6k Advanced Markers — NEEDS a mid-tier device to measure before changing the renderer.
9. Route/day planning — feature; NEEDS a product decision on scope.
10. Auth/RLS hardening — PARKED, needs explicit user go-ahead (STOP condition).

## Log
- 2026-07-01 — Follow-up push notifications (code complete; needs one-time setup, see docs/push-setup.md). Stack: `lib/push.ts` (VAPID key → bytes + subscription serialize, 5 tests), `push_subscriptions` table (SUPABASE.sql) + db helpers, SW `push`/`notificationclick` handlers (additive to public/sw.js), `NotifyToggle` in Follow-ups (subscribe/unsubscribe; hidden until push supported + VAPID key set), `/api/notify-followups` Vercel Cron (web-push daily digest of due follow-ups; CRON_SECRET-gated; proxy-excluded; prunes 404/410). 208 tests, tsc/lint(8)/build green. Blocked-on-user to activate: VAPID keys + env (Vercel), run the SQL, redeploy, grant permission on device. Delivery unverifiable here (no device/browser).
- 2026-07-01 — Route planning ("Plan my walk"): `lib/route.ts` greedy nearest-neighbour planner (5 tests) + `RoutePlanner` sheet (SheetShell, ariaLabel "Route") over not-knocked prospects from the user's location — numbered stops, per-leg/total distance, tap-to-open, per-stop Directions. Entry: a Route FAB on the map (`onOpenRoute` → `route` tab, mirrors Analytics). 203 tests, tsc/lint(8)/build green. Live UX unverified (browser) — user to eyeball.
- 2026-07-01 — Data safety: `scripts/backup.mjs` — on-demand backup (dated JSON in gitignored `backups/`, outside the DB) + `--restore <file>` (upsert by place_id, prospects first). Verified: backed up 2640 prospects + 8 pipeline rows. Mitigates the open anon-key/RLS wipe risk without the risky RLS lockdown. STILL OPEN: off-site + scheduled backups (blocked — Vercel project under an inaccessible team; Actions push needs `workflow` scope) and the real RLS hardening (needs browser to verify the app still works).
- 2026-06-30 — Canvassing-efficiency features (user-requested; personal tool). Foundation: `lib/geo.ts` (haversine + formatDistance, 13 tests) + `GeolocationProvider` (one shared GPS watcher; MapView migrated). Built all 4: (1) Search sorts nearest-first; (4) distance shown on Search/Follow-up rows + prospect card; (3) "Near me · not knocked" quick-filter chip (nearMe in filter model + matchesFilters, 1km radius, enables geo on tap); (2) one-tap stage change (stage chip = save+close, except lost). 198 tests, tsc/lint(8)/build green. Verified via build+tests + reasoning; live UX (GPS behavior, chip look) is the user's to eyeball — browser still not reachable from this session.
- 2026-06-30 — Accessibility pass (part 1): the 4 pull-up sheets (SheetShell: Search/Follow-ups/Contracts/Stats) and the prospect card are now labelled modal dialogs (role/aria-modal/aria-label) and close on Escape (SheetShell gained a window keydown handler; ProspectSheet already had one). Audit confirmed IconButtons, the search input, and Call/Directions buttons are already labelled. 3 new SheetShell a11y tests (180 total). tsc/lint(8)/build green. Deferred (need browser/AT): focus trap/restore, contrast measurement.
- 2026-06-30 — Offline-first Phase 3 (sync-status UI) built, completing the feature: `components/SyncStatus.tsx` — presentational top-left pill showing Offline / "N to sync" / Syncing…, `null` when online+idle+synced. Wired into MapView from DataProvider's `{online,pending,syncing}`. 5 jsdom tests (every state). Full gate green: tsc, 177 tests, next build. All 3 phases now shipped; end-to-end offline UX complete to spec but still needs device/browser verification (offline read render, edit-across-reload, flush-on-reconnect).
- 2026-06-30 — Offline-first Phase 1+2 built: DataProvider now reads the IndexedDB mirror first (SWR revalidate when online), saves optimistically to the outbox (no rollback on offline — flushes on `online`/foreground), and exposes `{online, pending, syncing}` for the deferred status UI. Chose idb + fake-indexeddb per sign-off. Lint steady (8), 172 tests, tsc + build green. NOT device-verified (needs browser). Phase 3 (indicator) pending browser connect.
- 2026-06-30 — Spec'd offline-first (docs/offline-first.md): IndexedDB read-through + SWR, optimistic save + place_id-keyed outbox, flush on online/foreground (no Background Sync — iOS), LWW single-tenant. Phased (reads → writes → status UI). Awaiting sign-off on 4 open questions (idb dep, write-failure semantics, indicator placement, scope) before building.
- 2026-06-30 — Component testing: added jsdom + @testing-library/react (per-file `// @vitest-environment jsdom`; `@` alias + matchMedia polyfill; inlined @mui/@emotion/react-transition-group to fix ESM directory import). First component test covers FilterPanel multi-select toggle + Clear-all. 157→160 tests, tsc/lint(8)/build green. Note: props-only components (FilterPanel, BottomNav) are cleanly testable this way; context/map/gesture components (MapView, ProspectSheet, …) still want the live browser.
- 2026-06-30 — Data quality: request Places `businessStatus` and skip CLOSED_PERMANENTLY/CLOSED_TEMPORARILY in both the scraper and the in-app pull (keep unknown-status). Takes effect on the next scrape/Refresh (existing rows unchanged; not re-scraping — quota). tsc/lint(8)/tests(157)/build green.
- 2026-06-30 — Test coverage: `lib/auth` (salted SHA-256 token derivation, null-when-unconfigured, deterministic, never leaks password) and extracted `directionsUrls()` from `lib/directions` (+ encoding tests). 148→157 tests. Lint 8, tsc + build green. Next: data-quality `businessStatus` in scraper; then evaluate offline-first / a11y (implement + build-verify only — no device here to confirm UX).
- 2026-06-30 — Extracted the prospect filter model + predicate to `lib/filters.ts` (`Filters`, `EMPTY_FILTERS`, `anyActiveFilter`, `matchesFilters`); MapView + FilterPanel now import it (removes the inline predicate + duplicated empty/any-active literals). +12 tests (148 total). Lint 8 (steady), tsc + build green.
- 2026-06-30 — react-hooks/refs (latest-value assigns): moved `ref.current = x` out of render into an effect in ClusteredMarkers (views/selected/onSelect), SheetShell + ProspectSheet (atFull). Verified readers run only in async handlers / later effects, so ordering is preserved. Lint 13→8 errors. Tests 136, tsc, build green. Remaining 8 = `set-state-in-effect` (legitimate patterns) — left as-is, NOT downgrading the rule to fake green.
- 2026-06-30 — react-hooks/refs: AppShell + MapView shared MotionValue now via lazy `useState(() => motionValue(0))` instead of `useRef(...).current` (identical stable instance, no gesture-logic change). Lint 15→13 errors. Tests 136, tsc, build green. Next: remaining refs (latest-value assigns in ClusteredMarkers/SheetShell/ProspectSheet → move to effect), then set-state-in-effect case review.
- (campaign start) Created CHANGELOG.md + this file. Baseline captured above.
- 2026-06-30 — Added Vitest (`npm test`) + 136 unit tests across contracts/stages/icp/analytics (pure logic; node env). tsc + build green.
- 2026-06-30 — Lint safe subset: Node-script `require` exemption, removed 2 dead `eslint-disable` directives, pruned unused `ICP` import in Contracts. 21→17 problems; tests/tsc/build green. Remaining 15 = react-hooks rules across interactive core (see queue #2) — deferred for careful per-file work. Next: react-hooks/refs latest-value fixes (mechanical, lowest-risk first: AppShell/MapView `useRef(motionValue()).current` → lazy `useState`), then expand tests toward component/integration coverage.
